$('.reviews').prepend("<%= j(render('review', :review => @review)) %>")


$('.review_<%= @review.id %> .rating-container-podcast div').raty({
  score: <%= @review.rating %>,
  path: 'assets',
  readOnly: true,
  starType: 'i'
});

$('.rating-container-big').addClass('orange');
